name: Deploy to Production Server

on:
  push:
    branches:
      - main  # Триггер на пуши и мерджи в main ветку

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.12'

      - name: Install dependencies
        run: |
          python -m venv .venv
          source .venv/bin/activate
          pip install -r requirements.txt

      - name: Set up SSH for deployment
        run: |
          mkdir -p ~/.ssh  
          echo "$PRIVATE_KEY" > ~/.ssh/id_rsa
          chmod 600 ~/.ssh/id_rsa
          ssh-keyscan -H -p 51821 185.189.58.71 >> ~/.ssh/known_hosts  

      - name: Deploy to production server
        env:
          PRIVATE_KEY: ${{ secrets.SSH_PRIVATE_KEY }}  # Приватный ключ из Secrets
          SERVER_USER: root  # Имя пользователя для SSH подключения
          SERVER_IP: 185.189.58.71  # IP сервера
        run: |
          ssh -i ~/.ssh/id_rsa -p 51821 $SERVER_USER@$SERVER_IP << 'EOF'
            echo "Deploying to production server..."
            cd /Users/vladratnikov/Code/bot_jyotish/src
            git pull origin main
            source .venv/bin/activate
            pip install -r requirements.txt
            systemctl restart jyotish_bot.service  # Перезапуск сервиса
          EOF
